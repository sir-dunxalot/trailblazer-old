{{label-field
  propertyBinding='view.property'
  textBinding='view.label'
}}

<div class="input_wrapper">

  {{#if view.isDatepicker}}
    {{input elementIdBinding='view.datepickerInputId' class='datepicker input'}}
  {{/if}}

  {{partial 'easy-form/input-controls'}}
</div>
