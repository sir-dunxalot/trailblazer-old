<li {{bind-attr class=':task_item task.completed controller.shouldShow::hidden'}}>

  <div class="task_name">
    {{capitalize-string task.name}}

    {{#if showPersonalToggle}}
      <span {{bind-attr class=':assignee_name showPersonal:fade-out:fade-in'}}>
        {{task.assignee.firstName}}
      </span>
    {{/if}}
  </div>

  <div class="task_buttons">

    {{#link-to 'task.edit' task class='sub'}}
      <span class="icon-pencil small sub"></span>
      <span class="hidden">Edit</span>
    {{/link-to}}

    <button
      {{bind-attr class=":complete_button task.completed:sub"}}
      {{action 'toggleTaskCompletion' task}}
      title="Complete this task"
    >
      {{#if task.completed}}
        <span class="icon-restart"></span>
        <span class="text">Restart</span>
      {{else}}
        <span class="icon-fire"></span>
        <span class="text">Blaze it</span>
      {{/if}}
    </button>

  </div>

</li>
