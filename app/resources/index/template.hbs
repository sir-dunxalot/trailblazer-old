<ul>
  {{#each feature in content}}
    <li>
      {{#link-to 'feature' feature}}
        {{feature.name}}
      {{/link-to}}

      <p>{{feature.id}}</p>

      <p>
        {{moment feature.startDate dateFormat}}
        -
        {{moment feature.endDate dateFormat}}
      </p>

      <ol>
        {{#each stage in feature.stages}}
          <li>{{stage.duration}}</li>
        {{/each}}
      </ol>
    </li>
  {{/each}}
</ul>
