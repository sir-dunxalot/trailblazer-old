import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:stage-type/new', 'StageTypeNewView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
