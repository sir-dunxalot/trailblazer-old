import numberOfWorkingDays from '../../../utils/number-of-working-days';
import { module, test } from 'qunit';

module('Unit | Utility | number of working days');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberOfWorkingDays();
  assert.ok(result);
});
