import escapeCss from '../../../utils/escape-css';
import { module, test } from 'qunit';

module('Unit | Utility | escape css');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = escapeCss();
  assert.ok(result);
});
