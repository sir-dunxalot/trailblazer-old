import { sizeIcon } from '../../../helpers/size-icon';
import { module, test } from 'qunit';

module('Unit | Helper | size icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sizeIcon(42);
  assert.ok(result);
});
